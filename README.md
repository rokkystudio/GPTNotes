# 🚀 GPT Notes

### 📱 ***GPT Notes*** — это Android приложение для хранения заметок

## ⚙️ Возможности

- 📂 Хранение заметок на устройстве Android (без сервера)
- 🌐 Доступ к заметкам через веб-сайт (нужен сервер)
- 🔄 Синхронизация заметок с личным сервером (MySQL + PHP)
- 🤖 Интеграция с ChatGPT (дополнительные ячейки памяти)

## 🤖 Интеграция с ChatGPT

- 📥 Загрузка заметок в память чата ChatGPT
- ✏️ Создание, редактирование и удаление заметок по команде
- 📒 Использование заметок как дневников, списков дел, хранилищ идей
- 💾 Сохранение итогов из чатов ChatGPT обратно в заметки

## 🧠 Работа с памятью ChatGPT (06.2025)
Память в ChatGPT помогает лучше подстраивать ответы под пользователя,
запоминая важную информацию для следующих разговоров.

## 🧩 Типы памяти ChatGPT

| Вид памяти                        | Где хранится        | Доступность                                                             | Что содержит                                              |
|---------------------------------|---------------------|-------------------------------------------------------------------------|-----------------------------------------------------------|
| 💬 **Контекст чата**             | Сервер, текущий чат | Доступна только ChatGPT. Стирается при закрытии/обновлении/смене модели | Вся переписка в рамках этого чата |
| 📚 **Долговременная память (@bio)** | Сервер OpenAI       | Доступна ChatGPT и по запросу в чат                                     | Пользовательские инструкции, важные факты, предпочтения  | Можно управлять через API, через команды, частично, не всегда полностью видна в интерфейсе |
| 🗂️ **Saved Memories (Интерфейс памяти)** | Сервер OpenAI       | Доступна ChatGPT и в настройках                                         | Важные блоки памяти, явно сохранённые или автоматически   | Просмотр/удаление через настройки, пользователь управляет вручную                          |
| 🕵️‍♂️ **Reference Chat History (История переписки в браузере)** | Локально в браузере | Доступна пользователю для просмотра. Частично оправляется на сервер.    | Используется для улучшения контекста, недоступна пользователю | Не управляется напрямую, отключена или ограничена в некоторых регионах                     |
| 🧑‍💼 **Память профиля (интерфейс персонализации)** | Сервер OpenAI       | Доступна ChatGPT и пользователю. Можно редактировать в настройках.      | Обобщённые пользовательские данные (имя, стиль, предпочтения) | Управляется в настройках персонализации                                                    |


### 📦 Блок памяти ChatGPT

- ❗ OpenAI не раскрывает точный размер одного блока.
- 📏 Размер блока может немного меняться и оценочно неточен.
- 🧠 Один блок — это единица хранения в памяти `@bio`.
- ✍️ Блок памяти обычно вмещает 1–2 предложения.
- 🔣 Приблизительно ~100–300 токенов (слова + символы).
- 📋 Может хранить простой факт, список дел или покупок.


### 📌 Контекст чата (локальная память)

- ⏱️ Временная память, работает только в текущем разговоре.
- 📡 Хранится на сервере во время активной сессии.
- 🔄 Помогает поддерживать связность и контекст диалога.
- 🔒 Недоступна для просмотра и редактирования.
- 🧺 Стирается при закрытии страницы, обновлении чата или смене модели.
- 🚫 Не сохраняется между сессиями и недоступна из других чатов.
- 📦 Ограничение — около 64 блоков (≈ 4000–8000 токенов).
- 👀 ChatGPT не видит твою историю переписки, если ты сам её не отправишь.


### 🗃 Память чата в браузере (Reference Chat History)
- 🔍 Помнит детали из прошлых разговоров, чтобы лучше понимать вас.
- 🧠 Использует этот контекст, даже если вы не сохраняли информацию специально.
- 💎 Доступно только в платных планах и можно выключить в настройках.

- 🐢 Большие диалоги сильно нагружают CPU и RAM
- При утрате `локальной памяти` может использоваться память браузера.
- Некоторые модели бесплатной версии не загружают утраченную память.


### 🗂️ Долговременная память (@bio)
 
- 💾 Запоминает когда вы просите сохранить важные детали из беседы.
- ⚙️ Посмотреть содержимое можно в чате если попросить "покажи память @bio".
- 📦 Размер памяти 16~64 блока (достоверно не известно).

- 🔄 Изменения создают новые блоки, старые не удаляются (05/2025)
- 📥 Загружается при каждом запросе в чат

### 🗂️ Долговременная память (Saved Memories) (05/2025)
- ⚙️ Расположена в `Настройки` → `Персонализация` → `Память`
- 📦 Размер памяти 16~20 блока (для бесплатной версии)
- Этот функционал памяти появился недавно и нестабилен.
- Пользователь может только просматривать и удалять блоки кнопкой `Забыть`.
- Нельзя редактировать блоки памяти через редактор.
- `ChatGPT` может только добавлять новые блоки.
- `ChatGPT` не может удалить .
- ⚠️ Память быстро переполняется из-за ограничений

### ⚙️ Технические детали

- ⏳ Бесплатно — максимум 2000 токенов (слов и символов) в одном разговоре.
- 🚀 Платно (Plus, Pro) — до 8000 токенов для более длинных диалогов.
- 💾 Вся сохранённая память и история хранятся на серверах OpenAI для лучшей работы.

### 💳 Доступность по подпискам

- 🆓 Бесплатно: доступна только долговременная память, история чатов — нет.
- 💎 Платно (Plus, Pro): доступны долговременная память + история чатов.
- ⚙️ Платные планы дают больше функций и удобств для управления памятью.

### 🌍 Региональные ограничения

- 🚫 В некоторых странах память может работать с ограничениями или выключена:  
  Страны Евросоюза, Швейцария, Норвегия, Исландия, Лихтенштейн.
- 📜 Это связано с местными законами и правилами.


### 🚧 Ограничения и рекомендации

- 💾 Сохраняйте данные и начинайте новый чат
- 🗂️ Можно попросить `ChatGPT` сгруппировать или сжать информацию
- ⚠️ «Оптимизировать» может исказить смысл — используйте осторожно

### 🛠 Как память `@bio` используется в приложении

- 🧩 Для работы нужна память `@bio`
- 📄 В память `@bio` добавляются инструкции взаимодействия
- 🔄 Каждый запрос загружает инструкции из памяти
- 🤝 `ChatGPT` взаимодействует с заметками через память





## 🔄 Как работает синхронизация

- 🌐 Изменения сразу отправляются на сервер при наличии интернета
- ⚔️ При конфликтах версий создаются дубликаты
- 🧹 ChatGPT может сравнить версии, удалить повторы и создать чистовую копию
- 💾 Данные сохраняются на устройстве или сервере, даже при недоступности одного из них

## 🗄️ Структура базы данных

Создаётся база `gptnotes` с двумя таблицами:

### 📄 Таблица `notes`

| Поле    | Тип         | Назначение                           |
|---------|-------------|------------------------------------|
| `id`    | INT         | Уникальный ID (AUTO_INCREMENT)     |
| `title` | VARCHAR(255)| Заголовок заметки                  |
| `text`  | TEXT        | Текст заметки                      |
| `tags`  | TEXT        | Теги (через запятую)               |
| `date`  | DATETIME    | Дата создания или обновления       |

### 🕒 Таблица `cache`

Буфер для передачи длинных заметок от ChatGPT через GET-запросы.

| Поле    | Тип          | Назначение                        |
|---------|--------------|---------------------------------|
| `id`    | INT          | Уникальный ID                   |
| `title` | VARCHAR(255) | Заголовок заметки               |
| `text`  | TEXT         | Содержимое                     |

# ⚡ Быстрый старт

**Импортируйте структуру базы данных на сервер.**  
Выберите способ установки:

## 🔧 Способ 1: через терминал

```bash
mysql -u MYSQL_USER -p < gptnotes.sql
```
После запуска запросит пароль.

### Если нужно создать нового пользователя MySQL
- Войдя в MySQL под root, сделай:
```bash
  mysql -u root -p
```
Введи пароль.

В MySQL-консоли введи команду:

```sql
CREATE USER 'newuser'@'localhost' IDENTIFIED BY 'password123';
GRANT ALL PRIVILEGES ON *.* TO 'newuser'@'localhost' WITH GRANT OPTION;
FLUSH PRIVILEGES;
```

* newuser — имя пользователя, которое хочешь создать.

* password123 — пароль для этого пользователя.

* localhost — разрешает подключение только с локальной машины (если нужен доступ с других компьютеров — меняй на %).


## 🔧 Способ 2: через phpMyAdmin

- Зайдите в phpMyAdmin (если он установлен)

- Создайте новую базу данных `gptnotes` с кодировкой `utf8mb4_general_ci`

- Перейдите в базу `gptnotes`

- Загрузите файл `gptnotes.sql` во вкладке `Импорт`

- Или же вы можете скопировать SQL-запрос и выполнить его во вкладке `SQL`

```sql
-- Создание базы данных
CREATE DATABASE IF NOT EXISTS gptnotes DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
USE gptnotes;

-- Основная таблица заметок
CREATE TABLE notes (
    id INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,  -- простой ключ
    title VARCHAR(255) NOT NULL,                          -- заголовок заметки
    text TEXT,                                            -- содержимое заметки
    tags TEXT,                                            -- строка с тегами (через запятую)
    date DATETIME DEFAULT CURRENT_TIMESTAMP               -- дата создания/обновления
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- Временный кэш команд
CREATE TABLE cache (
    id INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,  -- простой ключ
    title VARCHAR(255) NOT NULL,                          -- заголовок заметки
    cmd VARCHAR(255) NOT NULL,                            -- отложенная команда
    text TEXT                                             -- содержимое заметки
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```